from setuptools import setup, find_packages

setup(
    name="video_filters",
    version="0.1.0",
    packages=find_packages(),  # Automatically find all packages and subpackages
    description="A collection of video filters modules that we can run to generate different filter metrics for video.",
    author="Ruchit Rawal",
)
